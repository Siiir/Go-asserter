package asserter

// TestAsserter_Inc is redundant, since `Asserter.Inc` has already tests in a file "Types_test.go" .

// TestAsserter_IncLast is redundant, since `Asserter.IncLast` has already tests in a file "Types_test.go" .

// TestResetCounter is redundant, since `Asserter.ResetCounter` is simple and has tests in a file "Types_test.go" .